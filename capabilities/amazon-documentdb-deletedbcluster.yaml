naftiko: 1.0.0-alpha2
info:
  label: 'Amazon DocumentDB API — #DeleteDBCluster'
  description: 'Amazon DocumentDB API — #DeleteDBCluster. 1 operations. Lead operation: Amazon DocumentDB Delete DB Cluster.
    Self-contained Naftiko capability covering one Amazon Documentdb business surface.'
  tags:
  - Amazon Documentdb
  - '#DeleteDBCluster'
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_DOCUMENTDB_API_KEY: AMAZON_DOCUMENTDB_API_KEY
capability:
  consumes:
  - type: http
    namespace: amazon-documentdb-deletedbcluster
    baseUri: https://rds.amazonaws.com
    description: 'Amazon DocumentDB API — #DeleteDBCluster business capability. Self-contained, no shared references.'
    resources:
    - name: '#DeleteDBCluster'
      path: /#DeleteDBCluster
      operations:
      - name: deletedbcluster
        method: POST
        description: Amazon DocumentDB Delete DB Cluster
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Action
          in: query
          type: string
          required: true
        - name: Version
          in: query
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: Authorization
      value: '{{env.AMAZON_DOCUMENTDB_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: amazon-documentdb-deletedbcluster-rest
    port: 8080
    description: 'REST adapter for Amazon DocumentDB API — #DeleteDBCluster. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.'
    resources:
    - path: /v1/deletedbcluster
      name: deletedbcluster
      description: 'REST surface for #DeleteDBCluster.'
      operations:
      - method: POST
        name: deletedbcluster
        description: Amazon DocumentDB Delete DB Cluster
        call: amazon-documentdb-deletedbcluster.deletedbcluster
        with:
          Action: rest.Action
          Version: rest.Version
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: amazon-documentdb-deletedbcluster-mcp
    port: 9090
    transport: http
    description: 'MCP adapter for Amazon DocumentDB API — #DeleteDBCluster. One tool per consumed operation, routed inline
      through this capability''s consumes block.'
    tools:
    - name: amazon-documentdb-delete-db-cluster
      description: Amazon DocumentDB Delete DB Cluster
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: amazon-documentdb-deletedbcluster.deletedbcluster
      with:
        Action: tools.Action
        Version: tools.Version
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
