naftiko: 1.0.0-alpha2
info:
  label: 'Amazon DocumentDB API — #CreateDBInstance'
  description: 'Amazon DocumentDB API — #CreateDBInstance. 1 operations. Lead operation: Amazon DocumentDB Create DB Instance.
    Self-contained Naftiko capability covering one Amazon Documentdb business surface.'
  tags:
  - Amazon Documentdb
  - '#CreateDBInstance'
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_DOCUMENTDB_API_KEY: AMAZON_DOCUMENTDB_API_KEY
capability:
  consumes:
  - type: http
    namespace: amazon-documentdb-createdbinstance
    baseUri: https://rds.amazonaws.com
    description: 'Amazon DocumentDB API — #CreateDBInstance business capability. Self-contained, no shared references.'
    resources:
    - name: '#CreateDBInstance'
      path: /#CreateDBInstance
      operations:
      - name: createdbinstance
        method: POST
        description: Amazon DocumentDB Create DB Instance
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Action
          in: query
          type: string
          required: true
        - name: Version
          in: query
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: Authorization
      value: '{{env.AMAZON_DOCUMENTDB_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: amazon-documentdb-createdbinstance-rest
    port: 8080
    description: 'REST adapter for Amazon DocumentDB API — #CreateDBInstance. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.'
    resources:
    - path: /v1/createdbinstance
      name: createdbinstance
      description: 'REST surface for #CreateDBInstance.'
      operations:
      - method: POST
        name: createdbinstance
        description: Amazon DocumentDB Create DB Instance
        call: amazon-documentdb-createdbinstance.createdbinstance
        with:
          Action: rest.Action
          Version: rest.Version
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: amazon-documentdb-createdbinstance-mcp
    port: 9090
    transport: http
    description: 'MCP adapter for Amazon DocumentDB API — #CreateDBInstance. One tool per consumed operation, routed inline
      through this capability''s consumes block.'
    tools:
    - name: amazon-documentdb-create-db-instance
      description: Amazon DocumentDB Create DB Instance
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: amazon-documentdb-createdbinstance.createdbinstance
      with:
        Action: tools.Action
        Version: tools.Version
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
